extends ../layout

block content
  div(class="min-h-screen flex items-center justify-center py-12 px-4 sm:px-6 lg:px-8")
    div(class="max-w-md w-full space-y-8 rounded-l-md border border-r-0 border-gray-300 bg-gray-50 text-gray-500 shadow-lg p-8")
      div(class="text-center")
        h2(class="mt-6 text-3xl font-extrabold text-gray-900") Forgot your password?
        p(class="mt-2 text-sm text-gray-600") 
          | Enter your email address and we'll send you a link to reset your password.
      
      form(class="mt-8 space-y-6" action="/auth/forgot-password" method="POST")
        div(class="rounded-md shadow-sm -space-y-px")
          div
            label(for="email" class="sr-only") Email address
            div(class="flex" style="margin-top: 0.5rem;")
              span(class="inline-flex items-center px-3 rounded-l-md border border-r-0 border-gray-300 bg-gray-50 text-gray-500 p-4s")
                i(class="bx bx-envelope text-lg")
              input(id="email" name="email" type="email" required class="appearance-none rounded-none relative block w-full px-3 py-3 border border-gray-300 placeholder-gray-500 text-gray-900 rounded-r-md focus:outline-none focus:ring-indigo-500 focus:border-indigo-500 focus:z-10 sm:text-sm" placeholder="Email address")
        
        div
          button(type="submit" class="group relative w-full flex justify-center py-3 px-4 border border-transparent text-sm font-medium rounded-md text-white focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500" style="background-image: var(--primary-gradient)")
            span(class="absolute left-0 inset-y-0 flex items-center pl-3")
              i(class="bx bx-mail-send text-lg")
            | Send reset link
      
      div(class="text-center mt-4")
        p(class="text-sm text-gray-600")
          | Remember your password? 
          a(href="/auth/login" class="font-medium text-indigo-600 hover:text-indigo-500") Sign in 