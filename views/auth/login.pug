extends ../layout

block content
  div(class="min-h-screen flex items-center justify-center py-12 px-4 sm:px-6 lg:px-8")
    div(class="max-w-md w-full space-y-8 rounded-l-md border border-r-0 border-gray-300 bg-gray-50 text-gray-500 shadow-lg p-8")
      div(class="text-center")
        h2(class="mt-6 text-3xl font-extrabold text-gray-900") Sign in to ProMan
        p(class="mt-2 text-sm text-gray-600") 
          | Manage your projects with ease
      
      form(class="mt-8 space-y-6" action="/auth/login" method="POST")
        div(class="rounded-md shadow-sm -space-y-px")
          div
            label(for="username" class="sr-only") Username
            div(class="flex")
              span(class="inline-flex items-center px-3 rounded-l-md border border-r-0 border-gray-300 bg-gray-50 text-gray-500 p-4s")
                i(class="bx bx-user text-lg")
              input(id="username" name="username" type="text" required class="appearance-none rounded-none relative block w-full px-3 py-3 border border-gray-300 placeholder-gray-500 text-gray-900 rounded-r-md focus:outline-none focus:ring-indigo-500 focus:border-indigo-500 focus:z-10 sm:text-sm" placeholder="Username")
          
          div(class="mt-4")
            label(for="password" class="sr-only") Password
            div(class="flex" style="margin-top: 0.5rem;")
              span(class="inline-flex items-center px-3 rounded-l-md border border-r-0 border-gray-300 bg-gray-50 text-gray-500")
                i(class="bx bx-lock-alt text-lg")
              input(id="password" name="password" type="password" required class="appearance-none rounded-none relative block w-full px-3 py-3 border border-gray-300 placeholder-gray-500 text-gray-900 rounded-r-md focus:outline-none focus:ring-indigo-500 focus:border-indigo-500 focus:z-10 sm:text-sm" placeholder="Password")
        
        div(class="flex items-center justify-between")
          div(class="flex items-center")
            input(id="remember-me" name="remember-me" type="checkbox" class="h-4 w-4 text-indigo-600 focus:ring-indigo-500 border-gray-300 rounded")
            label(for="remember-me" class="ml-2 block text-sm text-gray-900") Remember me
          
          div(class="text-sm")
            a(href="/auth/forgot-password" class="font-medium text-indigo-600 hover:text-indigo-500") Forgot password?
        
        div
          button(type="submit" class="group relative w-full flex justify-center py-3 px-4 border border-transparent text-sm font-medium rounded-md text-white focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500" style="background-image: var(--primary-gradient)")
            span(class="absolute left-0 inset-y-0 flex items-center pl-3")
              i(class="bx bx-lock-alt text-lg")
            | Sign in
      
      div(class="text-center mt-4")
        p(class="text-sm text-gray-600")
          | Don't have an account? 
          a(href="/auth/register" class="font-medium text-indigo-600 hover:text-indigo-500") Sign up 